import { profileActions, profileReducer } from './model/slice/profileSlice'
import { type Profile, type ProfileSchema } from './model/types/profile'

export { type Profile, type ProfileSchema, profileActions, profileReducer }
